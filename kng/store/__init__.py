"""Embedded stores under `index/` — LanceDB vectors (WP2), graph (WP3).

Everything here is file-backed and copyable: the whole `index/` directory moves
to the query machine after WP3 with no server to stand up.
"""
